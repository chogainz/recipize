import { SwipeOffDirective } from './swipe-off.directive';

describe('SwipeOffDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeOffDirective();
    expect(directive).toBeTruthy();
  });
});
